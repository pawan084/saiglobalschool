import type { NextConfig } from "next";

// All site pages are now native React routes — no Wix HTML rewrites needed.
// The cloned HTML files remain in public/ for reference but are not the source of truth.
const nextConfig: NextConfig = {};

export default nextConfig;
